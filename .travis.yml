language: android
android:
  components:
    - tools
    - platform-tools
    - tools

    # The BuildTools version used by your project
    - build-tools-27.0.0

    # The SDK version used to compile your project
    - android-26
    
before_script: 
  - chmod +x gradlew
